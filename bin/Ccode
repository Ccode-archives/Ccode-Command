PY="3"
while getopts 'hp' flag; do
  case "${flag}" in
    h) printf "options: Ccode <options> <filename> \nflags: -h(help), -p(used to change python version to run with to 2)\n"
       exit 1 ;;
    p) PY="2";;
  esac
done
python$PY ~/Ccode/Ccode.py $1
